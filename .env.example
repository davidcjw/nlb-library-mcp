# NLB Catalogue API credentials — request at https://go.gov.sg/nlblabs-form
NLB_API_KEY=your-api-key
NLB_APP_CODE=your-app-code

# Optional override (defaults to the production v2 endpoint)
# NLB_API_BASE_URL=https://openweb.nlb.gov.sg/api/v2/Catalogue
